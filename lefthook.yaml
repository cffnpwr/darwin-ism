pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{rs,nix}"
      run: treefmt --fail-on-change {staged_files}
      stage_fixed: true

    lint:
      glob: "*.rs"
      run: cargo clippy --all
